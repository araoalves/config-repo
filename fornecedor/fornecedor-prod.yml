spring:
  datasource:
    username: dbuser
    password: admin2021
    platform: postgres
    initialization-mode: always
    url: jdbc:postgresql://144.22.188.2:5432/fornecedor
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: update
